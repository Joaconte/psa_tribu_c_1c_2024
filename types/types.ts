export interface Recurso {
  legajo: string;
  nombre: string;
  apellido: string;
}

export interface Project {
  projectCode: string;
  leaderCode: string;
  productCode: string;
  name: string;
  startDate: string;
  endDate: string;
  description: string;
  status: string;
}